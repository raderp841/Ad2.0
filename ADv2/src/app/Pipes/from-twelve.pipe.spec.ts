import { FromTwelvePipe } from './from-twelve.pipe';

describe('FromTwelvePipe', () => {
  it('create an instance', () => {
    const pipe = new FromTwelvePipe();
    expect(pipe).toBeTruthy();
  });
});
